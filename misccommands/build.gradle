dependencies {
    compile project(":core")
    compile "org.jetbrains.kotlin:kotlin-stdlib-jdk8:$kotlin_version"
}

jar {
    archiveName = "misccommands.jar"
    destinationDir project.file('../deploy/modules')
}